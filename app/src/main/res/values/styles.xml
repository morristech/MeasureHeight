<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/colorActivityBackground</item>
        <item name="android:textColorPrimary">@color/colorbDefaultText</item>
    </style>

    <style name="textDefault" parent="@android:style/Widget.TextView">
        <item name="android:textSize">@dimen/font_size_normal</item>
        <item name="android:textColor">@color/colorbDefaultText</item>
        <item name="android:ellipsize">end</item>
        <item name="android:lines">1</item>
        <item name="android:singleLine">true</item>
    </style>

    <style name="textSize" parent="textDefault">
        <item name="android:textSize">@dimen/font_size_small</item>
        <item name="android:textColor">@color/colorSize</item>
    </style>

    <style name="btnDefault" parent="Base.Widget.AppCompat.Button">
        <item name="android:background">@drawable/btn_default_bg</item>
        <item name="android:textSize">@dimen/font_size_normal</item>
        <item name="android:textColor">@color/colorbBtnText</item>
        <item name="android:minHeight">0dp</item>
    </style>

    <style name="NormalCheckBox" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:button">@drawable/refer_check_bg</item>
    </style>

    <style name="editTextDefault" parent="@android:style/Widget.EditText">
        <item name="android:textColor">@color/colorEditText</item>
        <item name="android:textColorHint">@color/colorEditHint</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:padding">8dp</item>
        <item name="android:textSize">@dimen/font_size_normal</item>
        <item name="android:singleLine">true</item>
    </style>


</resources>
